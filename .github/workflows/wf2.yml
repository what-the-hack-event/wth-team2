on:
  push:
    branches: [team_syuria]
    
jobs:
  job1:
      runs-on: ubuntu-latest
      steps:
        - run: |
            echo "step 1 run"
  job2:
      runs-on: ubuntu-latest
      steps:
        - run: |
            echo "step 2 run"
